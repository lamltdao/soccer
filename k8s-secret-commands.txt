# Run commands on the first time codes are shipped to a new cluster
kubectl create secret generic jwt-secret --from-literal=JWT_SECRET=SECRET
kubectl create secret generic mongo-atlas-password --from-literal=MONGO_ATLAS_PASSWORD=auth
